/** Fixed homepage hero headline — demo-website positioning. */
export function HeroHeadlines() {
  return (
    <h1
      id="hero-heading"
      className="mt-3 text-3xl font-bold leading-tight tracking-tight text-slate-900 dark:text-white sm:text-4xl md:text-5xl lg:text-[2.75rem] lg:leading-[1.12]"
    >
      Find the businesses that need you — then show them exactly what they&apos;re missing.
    </h1>
  );
}
